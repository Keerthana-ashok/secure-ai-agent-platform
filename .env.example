LLM_API_KEY=
MODEL_NAME=your-model-name
TEMPERATURE=0.7
MAX_TOKENS=256
PROVIDER_URL=https://api.example.com/v1/generate
TIMEOUT=10

# Copy `.env.example` to `.env` and fill values for local development.